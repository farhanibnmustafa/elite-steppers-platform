/**
 * Example: drop into `src/app/page.tsx` (App Router) after copying
 * `templates/elite-steppers-landing/src` → e.g. `src/components/elite-steppers-landing/`.
 *
 * Adjust the import path to match your project alias.
 */
import { EliteSteppersLanding } from "@/components/elite-steppers-landing";

export default function HomePage() {
  return <EliteSteppersLanding logoPriority />;
}
