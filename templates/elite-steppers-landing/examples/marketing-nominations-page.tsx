/**
 * Example: `src/app/(marketing)/nominations/page.tsx`
 *
 * Add `fontYouRock` in `src/app/fonts.ts` (see `snippets/font-you-rock.ts`) if you
 * use the success route; the nominations page does not need it.
 */
import { NominationsPageContent } from "@/components/elite-steppers-landing";

export default function NominationsPage() {
  return <NominationsPageContent />;
}
