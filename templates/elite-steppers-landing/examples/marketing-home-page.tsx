/**
 * Example: `src/app/(marketing)/page.tsx` — home body only; header comes from the layout.
 */
import { EliteSteppersLandingSections } from "@/components/elite-steppers-landing";

export default function HomePage() {
  return <EliteSteppersLandingSections />;
}
