/**
 * Metadata and optional copy when porting. Hero, form, and impact panel text live
 * in `DonateHero`, `DonatePageSections`, and `DonateGiftForm` (see production source).
 */
export const donatePageConfig = {
  metadata: {
    title: "Donate | Elite Level Steppers",
    description:
      "Support programs, archives, and events that preserve stepping culture.",
  },
} as const;
